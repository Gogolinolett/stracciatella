# Miner Module — Design Decisions (archiviert)

> **Diese Datei ist ein Archiv-Stub.** Die Design-Entscheidungen dieses Moduls
> liegen jetzt als Tickets in der Wissensdatenbank
> (`knowledge-manager/Vault`), nicht mehr hier. Der frühere Volltext bleibt in
> der Git-Historie.

Nicht in dieser Datei nachlesen, sondern suchen — vom Repo-Root:

```powershell
.\knowledge-manager\gradlew.bat -p knowledge-manager run --quiet --args="search 'Suchbegriffe' 3"
```

Tickets dieses Moduls: **STR-047 bis STR-052** — Vorwärtsbewegung über den
Collect-Walk statt eigener Movement-Tasks, Fail-Fast bei Hazards,
Ore-Gewinnung über begehbare 1x2-Galerien, sequentielle Batches statt
Bulk-Plan, Ore-Chasing erst auf Tunnelhöhe und die 3-Block-Treppengeometrie
beim Abstieg.
